# Atreides drill soldier

11 delivered RGBA animation strips, 65 frames. Each cell is 400 x 336; metadata preserves the specified FPS and loop flags. The canonical reference was approved by the user.

Open `generated/review/index.html` for playback, frame stepping, game-size viewing and horizontal mirroring. `generated/review/overview.png` compares representative frames with the approved reference. GIF previews repeat for review, with an extended last-frame hold for one-shot actions; their timing is rounded to centiseconds. Use the viewer for specified FPS and permanent final-pose holds.

Dimensions, transparency, nonempty/distinct cells, cell-edge clearance, hashes and runtime playback metadata were checked. Sources, packed frames and game-size sheets were visually inspected. Playback, loop seams and transitions have NOT been verified; the user explicitly authorized manual playback review. Reload took three attempts to repair visible magazine continuity; the rifle's release/internal mechanism remains unspecified.

The generation set is complete. Overall verification remains incomplete until manual playback review. See `atreides_drill_soldier_animation_manifest.json` for per-animation notes, and `generated/generation_prompts.json` for the built-in image-generation prompt set. Original generated sources are under `generated/source/`. No gameplay integration was performed.
